const fs = require("fs");

const { readdir, readFile } = require("../src/file-utils.js");

describe("Node.fs", function() {
  describe("readdir", function() {
    // it('returns the files in such folder', function(done) {
    //     fs.readdir('../node_modules/request', function(err, result) {
    //         if (err) throw err;
    //         console.log('readdir', result);
    //         done();
    //     });
    // });
    // it("throws exception when path is not found", function(done) {}) {
    //
    //  // Jasmine doesn't support throwing exceptions within async calls, need a custom matcher
    // }
  });
});
